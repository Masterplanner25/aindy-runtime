# A.I.N.D.Y. Watcher — server-side signal constants.
#
# The watcher client process lives in aindy-sdk: aindy_sdk/watcher/
# This package retains only the signal/activity type constants used by
# the runtime's signal ingestion endpoint (watcher_router) and syscall contract.
