__pycache__/
*.py[cod]
.pytest_cache/
.coverage
coverage.xml
coverage-*.xml
.venv/
build/
dist/
*.egg-info/
.env
AINDY/.env
# The five design/audit docs formerly listed here are now committed (2026-08-05).
# They were cited from tracked source -- effect_record.py, alembic 0002/0003, and
# sandbox_runner.py all name them -- so excluding them left those citations pointing at
# files no clone contained. `Idempotency convo.txt` stays out: it is a raw transcript,
# not a document.
Idempotency convo.txt
logs/
test_c2_verification/
.claude/settings.local.json
clean/

# Personal working/handoff notes (not part of the repo)
Session Handoff Summary.md

# Nodus stdlib compiled bytecode cache (regenerated on demand)
.nodus/cache/

# Nodus workflow runtime state (graphs/checkpoints) written when workflows run
/.nodus/
